{
  "name": "expensetracker",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend/expense-tracker",
    "build": "npm run build --prefix frontend/expense-tracker",
    "start": "npm run start --prefix backend"
  }
}
